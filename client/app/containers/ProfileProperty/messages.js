/*
 * ProfileProperty Messages
 *
 * This contains all the text for the ProfileProperty container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProfileProperty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileProperty container!',
  },
});
