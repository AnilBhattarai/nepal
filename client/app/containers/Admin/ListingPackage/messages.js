/*
 * ListingPackage Messages
 *
 * This contains all the text for the ListingPackage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListingPackage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListingPackage container!',
  },
});
