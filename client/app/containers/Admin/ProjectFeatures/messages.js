/*
 * ProjectFeatures Messages
 *
 * This contains all the text for the ProjectFeatures container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProjectFeatures';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProjectFeatures container!',
  },
});
