/*
 * LocationTreeView Messages
 *
 * This contains all the text for the LocationTreeView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LocationTreeView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LocationTreeView container!',
  },
});
