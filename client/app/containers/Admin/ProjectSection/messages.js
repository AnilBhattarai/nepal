/*
 * ProjectSection Messages
 *
 * This contains all the text for the ProjectSection container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProjectSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProjectSection container!',
  },
});
