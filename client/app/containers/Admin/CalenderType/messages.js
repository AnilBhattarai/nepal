/*
 * CalenderType Messages
 *
 * This contains all the text for the CalenderType container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CalenderType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CalenderType container!',
  },
});
