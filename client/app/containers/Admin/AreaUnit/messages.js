/*
 * AreaUnit Messages
 *
 * This contains all the text for the AreaUnit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AreaUnit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AreaUnit container!',
  },
});
