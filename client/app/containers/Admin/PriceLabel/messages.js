/*
 * PriceLabel Messages
 *
 * This contains all the text for the PriceLabel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PriceLabel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PriceLabel container!',
  },
});
