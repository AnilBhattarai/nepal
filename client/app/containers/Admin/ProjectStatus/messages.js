/*
 * ProjectStatus Messages
 *
 * This contains all the text for the ProjectStatus container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProjectStatus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProjectStatus container!',
  },
});
