/*
 * Municipality Messages
 *
 * This contains all the text for the Municipality container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Municipality';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Municipality container!',
  },
});
