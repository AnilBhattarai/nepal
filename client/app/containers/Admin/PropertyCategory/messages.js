/*
 * PropertyCategory Messages
 *
 * This contains all the text for the PropertyCategory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PropertyCategory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PropertyCategory container!',
  },
});
