/*
 * ComplainType Messages
 *
 * This contains all the text for the ComplainType container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComplainType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ComplainType container!',
  },
});
