/*
 * OfferMessages Messages
 *
 * This contains all the text for the OfferMessages container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OfferMessages';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OfferMessages container!',
  },
});
