/*
 * ImageCaption Messages
 *
 * This contains all the text for the ImageCaption container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ImageCaption';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageCaption container!',
  },
});
