/*
 * RequestManagement Messages
 *
 * This contains all the text for the RequestManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestManagement container!',
  },
});
