/*
 * PropertyPurpose Messages
 *
 * This contains all the text for the PropertyPurpose container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PropertyPurpose';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PropertyPurpose container!',
  },
});
