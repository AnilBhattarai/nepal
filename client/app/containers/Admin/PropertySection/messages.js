/*
 * PropertySection Messages
 *
 * This contains all the text for the PropertySection container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PropertySection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PropertySection container!',
  },
});
