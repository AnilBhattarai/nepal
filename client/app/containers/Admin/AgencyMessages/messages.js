/*
 * AgencyMessages Messages
 *
 * This contains all the text for the AgencyMessages container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgencyMessages';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgencyMessages container!',
  },
});
