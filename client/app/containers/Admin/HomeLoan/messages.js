/*
 * HomeLoan Messages
 *
 * This contains all the text for the HomeLoan container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeLoan';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeLoan container!',
  },
});
