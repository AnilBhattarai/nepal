/*
 * PropertyFace Messages
 *
 * This contains all the text for the PropertyFace container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PropertyFace';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PropertyFace container!',
  },
});
