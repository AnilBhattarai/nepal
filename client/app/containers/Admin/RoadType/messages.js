/*
 * RoadType Messages
 *
 * This contains all the text for the RoadType container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RoadType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoadType container!',
  },
});
