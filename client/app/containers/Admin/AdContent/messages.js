/*
 * AdContent Messages
 *
 * This contains all the text for the AdContent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdContent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdContent container!',
  },
});
