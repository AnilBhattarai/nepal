/*
 * Property Messages
 *
 * This contains all the text for the Property container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Property';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Property container!',
  },
});
