/*
 * AgentPage Messages
 *
 * This contains all the text for the AgentPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgentPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgentPage container!',
  },
});
