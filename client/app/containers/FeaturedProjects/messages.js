/*
 * FeaturedProjects Messages
 *
 * This contains all the text for the FeaturedProjects container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FeaturedProjects';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeaturedProjects container!',
  },
});
