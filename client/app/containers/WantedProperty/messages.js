/*
 * WantedProperty Messages
 *
 * This contains all the text for the WantedProperty container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WantedProperty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WantedProperty container!',
  },
});
