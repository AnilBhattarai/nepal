/*
 * HomeComponents Messages
 *
 * This contains all the text for the HomeComponents container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeComponents';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeComponents container!',
  },
});
