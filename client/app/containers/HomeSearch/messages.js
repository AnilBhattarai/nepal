/*
 * HomeSearch Messages
 *
 * This contains all the text for the HomeSearch container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeSearch container!',
  },
});
