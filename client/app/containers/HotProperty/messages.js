/*
 * HotProperty Messages
 *
 * This contains all the text for the HotProperty container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HotProperty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HotProperty container!',
  },
});
