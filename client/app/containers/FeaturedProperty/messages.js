/*
 * FeaturedProperty Messages
 *
 * This contains all the text for the FeaturedProperty container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FeaturedProperty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeaturedProperty container!',
  },
});
