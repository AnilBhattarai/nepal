/*
 * FavoriteProperties Messages
 *
 * This contains all the text for the FavoriteProperties container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FavoriteProperties';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FavoriteProperties container!',
  },
});
